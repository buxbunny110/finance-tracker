<%= render "layouts/messages" %>

<% if @stock %>
  <div class="card card-header result-card">
    <strong>Stock:</strong> <%= @stock.ticker %>,
    <strong>Company name:</strong> <%= @stock.name %>,
    <strong>Last Price:</strong> <%= @stock.last_price %>
  </div>
<% end %>